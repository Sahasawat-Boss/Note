# Clone Repo and remove connetion

1. Clone Repo
2. Remove Git connection (.git folder)
```
Remove-Item -Recurse -Force .git
```
3. Initialize a new Git repository
```
git init
```
4.Done! >> Connect your local repo to the new GitHub repo
